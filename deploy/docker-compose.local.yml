services:
  greenstreem-web-player:
    build:
      context: ..
      dockerfile: Dockerfile
    container_name: greenstreem-web-player
    restart: unless-stopped
    environment:
      GREENSTREEM_HOST: 0.0.0.0
      GREENSTREEM_PORT: 8097
      GREENSTREEM_SESSION_TTL_SECONDS: 43200
    ports:
      - "8097:8097"
